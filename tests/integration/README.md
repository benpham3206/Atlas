# Integration Tests

Cross-app integration tests belong here once Atlas has behavior that spans the API, web app, database, or agent tools.

The initial skeleton uses focused tests inside each package and app.
