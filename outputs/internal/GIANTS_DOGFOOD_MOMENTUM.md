# Giants-shoulders dogfood (momentum program)

**Problem:** Polish bought shelf + gates; Matrix/Paperclip/Hermes inspiration stayed mostly docs. Momentum = **cadence + one traced company turn**, not more markdown.

**Principle:** Atlas = authority. Hermes = harness + delivery. Matrix = loop semantics. Paperclip = Board + heartbeat. **No new servers** (Algorithm).

**Status dashboard:** `outputs/docs/FLYWHEEL_STATUS.md` · **110% verify:** `npm run flywheel:check`

## Four rhythms (110%)

| Rhythm | Giant | Cadence | Artifact |
|--------|-------|---------|----------|
| **Pulse** | Paperclip heartbeat | 2×/day Hermes cron → Photon | `npm run momentum:pulse` + Ben inch from **Now** (Doc Do) |
| **Turn** | Matrix company loop | 1×/week Mon 10am (attach_to_session) | PA-* gate + `TASKS.md` **or** personal complete + real `artifact_uri` |
| **Compound** | Hermes capability flywheel | Daily 7am | One `skill_manage` patch + one ≤20m Ben brick (same Doc Do) |
| **Watch** | Paperclip + gateway | 90m API + 20m Photon | `atlas-momentum-watchdog.sh` + `photon-imessage-watchdog.sh` |

## Daily pulse (Paperclip + Hermes)

1. `launchd` / `:4000` up (`npm run daemon:handoff` if needed).
2. Hermes cron **Atlas Giants momentum** runs skills `atlas-personal-dogfood`, `giants-shoulders-atlas-dogfood`.
3. Agent: `npm run momentum:pulse`, read `NEXT_ACTION.md`, **Ben inch = Now line** (during PA-* sprint, not ontology spine title), **execute one** Hermes upgrade (patch skill / cron / gate). Photon reply ≤6 lines.

Watchdog (no tokens): `~/.hermes/scripts/atlas-momentum-watchdog.sh` → only speaks if API down.

## Weekly company turn (Matrix)

**Public Atlas sprint (PA-*):**

1. **Owner** — confirm `NEXT_ACTION.md` / `TASKS.md` active PA row.
2. **Lead** — brick from `docs/bricks/2026-06-30-public-atlas-atomic-tasks.md`.
3. **Worker** — **Cursor/Codex** implements; Hermes does not own bulk import.
4. **System** — `npm test`, Paperclip boot smoke when in P1; `audit/verify` on legacy `:4000` if still running.
5. **Proof** — check PA-* box in `TASKS.md`; append epic line to `CONTEXT_LOG.md` (optional); update `NEXT_ACTION.md` when task advances.

**Personal spine (when PS-* unblocked):**

Same as before: `GET /personal/next-action`, gates, real evidence on complete — never smoke placeholders.

CI still runs `npm run demo:flagship` (synthetic workspace).

## Scorecard target (4 weeks → 120%)

Raise `POLISH_SCORECARD.md` **flywheel** + metaphor rows to **≥2**. Requires: all crons in `FLYWHEEL_STATUS.md` live + 4 weekly turns with evidence.

## Anti-patterns (high-agency)

- Architecture essays without `dev:personal` or `flywheel:check`.
- Marking personal tasks done from smoke tests.
- Heartbeat docs without `hermes cron` entry.
- Pulse that pushes ontology spine while Doc Do says PA-P*.
- New UI before pulse + turn habits stick.

## Pointers

- `outputs/docs/FLYWHEEL_STATUS.md` — cron inventory + tiers
- `outputs/docs/HEARTBEAT_CRON.md` — template (superseded by this program + Hermes skill)
- `outputs/demos/FLAGSHIP_LOOP.md` — synthetic loop reference
- Skill: `giants-shoulders-atlas-dogfood` (Hermes profile)