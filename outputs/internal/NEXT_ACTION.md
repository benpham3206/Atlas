# Atlas Internal Next Action

## Next Action

**Do:** Create the first customer-facing Atlas site/app output slice under `outputs/site/` or
`outputs/app/`.
**Because:** `outputs/` is now correctly framed as the customer-facing product shelf, but it still
contains descriptions rather than a polished public Atlas surface comparable to `matrix.build`.
**Context:** Start with `outputs/site/README.md`, `outputs/app/README.md`, and
`outputs/docs/LONG_RUNNING_WORK.md`.

## Update Rule

- Update this file at the end of each meaningful session.
- Keep it to one action, one reason, and one context pointer.
- If stale, recover from `outputs/internal/STATE.md`, then `TASKS.md`, then `CONTEXT_LOG.md`.

